const NotFound = () => {
  const style = {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "50vh",
    fontSize: "30px",
  };

  return <p style={style}>Page not found!</p>;
};

export default NotFound;
